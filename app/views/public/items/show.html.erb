<div class="container">
  <div class="row">
    <div class="col-md-6">
      <!-- 商品画像（後で実装） -->
      <div class="bg-secondary text-white text-center p-5">
        商品画像
      </div>
    </div>
    
    <div class="col-md-6">
      <h2><%= @product.name %></h2>
      <p><%= @product.introduction %></p>
      <h4>¥<%= number_to_currency(@product.price, unit: "", precision: 0) %> (税込)</h4>
      
      <%# カート機能実装時に有効化 %>
      <%#= form_with model: @cart_item, url: cart_items_path, local: true do |f| %>
        <%#= f.hidden_field :product_id, value: @product.id %>
        <%# <div class="form-group"> %>
          <%#= f.label :amount, "個数" %>
          <%#= f.number_field :amount, value: 1, min: 1, class: "form-control" %>
        <%# </div> %>
        <%#= f.submit "カートに入れる", class: "btn btn-success btn-lg" %>
      <%# end %>
      
      <p class="text-muted">※カート機能は実装中です</p>
    </div>
  </div>
</div>

